openapi: 3.1.0
info:
  title: Country Code Schema
  version: 1.0.0
components:
  schemas:
    CheckinRequestData:
      type: object
      properties:
        sessionId:
          type: string
          required: true
          format: uuid
        userId:
          type: string
          required: true
          format: uuid
        country:
          $ref: './shared.yaml#/components/schemas/CountryCode'
        scanToken:
          type: string
          description: session token generated by identity provider
        documentId:
          type: string
          description: Document identifier (must be 12 characters)
        providedFields:
          type: object
          additionalProperties:
            type: string
          description: A map of required fields and their corresponding values.
